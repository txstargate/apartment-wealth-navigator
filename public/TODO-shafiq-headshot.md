TODO-Shafiq: drop in final headshot.

`shafiq-hirani.jpg` in this directory is a 1x1 placeholder pixel, not a
real photo. Replace it with the final headshot (same filename, or update
the `image` field in `src/data/person-professionalservice.json` and
`schema/person-professionalservice.json` if the filename changes).
