---
const navItems = [
	{ label: 'Home', href: '/' },
	{ label: 'About', href: '/about' },
	{ label: 'Services', href: '/services' },
	{ label: 'Insights', href: '/insights' },
	{ label: 'Tools', href: '/tools' },
	{ label: 'Contact', href: '/contact' },
]

// TODO-Shafiq: confirm the phone number to publish here.
const phoneNumber = '(000) 000-0000'
const phoneHref = 'tel:+10000000000'
---

<header class="site-header">
	<nav class="site-nav">
		<a class="site-nav__brand" href="/">
			<svg
				class="site-nav__mark"
				xmlns="http://www.w3.org/2000/svg"
				width="32"
				height="32"
				viewBox="0 0 64 64"
				fill="none"
				role="img"
				aria-label="Apartment Wealth Navigator compass mark"
			>
				<circle cx="32" cy="32" r="29" stroke="#0F172A" stroke-width="1.5"></circle>
				<line x1="32" y1="6" x2="32" y2="11" stroke="#0F172A" stroke-width="1.5"></line>
				<line x1="32" y1="53" x2="32" y2="58" stroke="#0F172A" stroke-width="1.5"></line>
				<line x1="6" y1="32" x2="11" y2="32" stroke="#0F172A" stroke-width="1.5"></line>
				<line x1="53" y1="32" x2="58" y2="32" stroke="#0F172A" stroke-width="1.5"></line>
				<polygon points="32,10 29,32 32,33" fill="#C5A880"></polygon>
				<polygon points="32,10 35,32 32,33" fill="#A98C63"></polygon>
				<polygon points="32,54 29,32 32,31" fill="#0F172A"></polygon>
				<polygon points="32,54 35,32 32,31" fill="#334155"></polygon>
				<circle cx="32" cy="32" r="2.6" fill="#0F172A"></circle>
			</svg>
			<span class="site-nav__wordmark">Apartment Wealth Navigator</span>
		</a>
		<ul class="site-nav__links">
			{
				navItems.map((item) => (
					<li>
						<a href={item.href}>{item.label}</a>
					</li>
				))
			}
		</ul>
		<a class="site-nav__phone" href={phoneHref}>{phoneNumber}</a>
	</nav>
</header>

<style>
	.site-header {
		position: sticky;
		top: 0;
		z-index: 50;
		background: var(--color-bg);
		border-bottom: 1px solid var(--color-border);
	}

	.site-nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-4);
		max-width: 1200px;
		margin: 0 auto;
		padding: var(--space-3) var(--space-4);
	}

	.site-nav__brand {
		display: flex;
		align-items: center;
		gap: var(--space-2);
		font-family: var(--font-headline);
		font-weight: 600;
		color: var(--color-obsidian);
		text-decoration: none;
		white-space: nowrap;
	}

	.site-nav__mark {
		display: block;
		flex-shrink: 0;
	}

	.site-nav__wordmark {
		line-height: 1;
	}

	.site-nav__links {
		display: flex;
		gap: var(--space-4);
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.site-nav__links a {
		color: var(--color-text);
		text-decoration: none;
		font-size: var(--text-sm);
	}

	.site-nav__links a:hover {
		color: var(--color-accent);
	}

	.site-nav__phone {
		color: var(--color-obsidian);
		text-decoration: none;
		font-size: var(--text-sm);
		white-space: nowrap;
	}
</style>
